# data_shaft
Cornerstone for building a robust and decoupled Data Layer within a Clean Architecture setup. This package provides all the essential abstractions (interfaces and base models) required to define the contract of the Data Layer, without concerning itself with concrete implementation details.
